# core.volume_rendering

VTK pipeline: imports a numpy array, applies transfer functions, sets up
three-point lighting, and calls `Render()`.

---

::: medvol.core.volume_rendering
    options:
      members:
        - render_volume
      show_source: true
